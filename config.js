// ⚠️ CONFIGURACIÓN CENTRALIZADA
// Las URLs son públicas, pero NO contienen credenciales sensibles
// Las credenciales están protegidas en el backend (.env)

const CONFIG = {
  API_URL: "https://backendgunar-1.onrender.com"
};

// No expongas nunca claves API o credenciales aquí
// El backend es responsable de proteger acceso a Supabase

